document.getElementById('tecnologia-link').addEventListener('click', function(event) {
    event.preventDefault(); // Evita que o link abra uma nova página

    // Mostra o exemplo do Bootstrap
    document.getElementById('bootstrap-example').style.display = 'block';
});
